databases:
  - name: mancoba
    databaseName: mancoba_mqab
    user: mancoba_user

services:
  - type: web
    name: xpenses-list-dev
    env: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: mancoba_mqab
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false